# 有理牛奶冻（Rational Blancmange）

## 题目描述

定义牛奶冻函数（Blancmange function）$B(x)$ 为：
$$B(x) = \sum_{n=0}^{\infty} \frac{s(2^n x)}{2^n}$$
其中 $s(x)$ 表示 $x$ 到最近整数的距离。

对于有理数输入 $x = p/q$（$p, q$ 互质），计算 $B(p/q)$ 的值，并对某范围内的所有有理数求和。

## 输入格式

无输入（固定参数问题）。

## 输出格式

输出一个浮点数，表示最终答案。

## 样例

（无样例输入输出，直接计算并输出最终答案。）
