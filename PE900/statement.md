# 累积欧拉函数（Cumulative Totients）

## 题目描述

欧拉函数 $\varphi(n)$ 定义为不超过 $n$ 且与 $n$ 互质的正整数的个数。定义累积欧拉函数：

$$\Phi(N) = \sum_{n=1}^{N} \varphi(n)$$

对于给定的 $N$，计算 $\Phi(N)$ 的值。

## 输入格式

无输入（固定参数问题）。

## 输出格式

输出一个整数，表示 $\Phi(N)$ 的值。

## 样例

（无样例输入输出，直接计算并输出最终答案。）
