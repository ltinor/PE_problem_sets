---
name: project-euler-adapter
description: |
  你负责将 Project Euler 的原题改编为 OI/ACM 风格的试题。核心流程：从
  `source/_posts/` 读取 Project Euler 原题 → 提炼可用于 OI 出题的 idea →
  探索多个改编方向并记录取舍理由 → 选定一个方向完整落地（代码 + 数据 + 题面 + README）。
  所有产物使用 `_template/` 定义的文件结构和格式。
  触发关键词：改编 Project Euler、出 PE 题、PE 改编、Project Euler 出题、造 OI 题
user-invocable: true
---

# Project Euler → OI 试题改编

你将 Project Euler 的原题改编为 OI/ACM 风格的试题，产物格式遵循
`_template/` 的结构。

## 项目结构速览

```
ProjectEuler/
├── _template/          ← 题目模板（复制此目录开始新题）
│   ├── code/           ← std.cpp, brute.cpp, gen.cpp, gen_data.cpp
│   ├── data/           ← *.in, *.out（生成后）
│   ├── scripts/        ← check.sh, gen_out.sh
│   ├── eval.sh         ← 标程运行时间检测
│   ├── Makefile        ← 编译 + 数据生成 + 对拍
│   ├── statement.md    ← 中文题面
│   └── README.md       ← 改编说明（原题意 + 题目 + 数据范围 + 算法 + 改编思路记录）
├── source/_posts/      ← Project Euler 原题（双语 markdown）
├── 001/ ~ 968/         ← 已改编 / 待改编的题目
└── README.md           ← 项目总说明与改编策略
```

## 触发场景

当用户说"改编第 N 题"、"从 Project Euler 出 X 题"、"把 PE 的某题改编成 OI 题"时，
使用本 skill。也适用于批量改编多个题目。

## 工作流程

按以下顺序执行；每一步完成后再进入下一步。

### 第一步：读取原题

1. 从 `source/_posts/<N>.md` 读取 Project Euler 第 N 题。
   - 文件包含英文原文 + 中文翻译，均可作为理解题意的依据。
   - 提取：标题、问题描述、原题输入（通常是固定常数）、原题输出。
2. 理解原题的核心数学模型或算法问题是什么。

### 第二步：改编方向头脑风暴

这是关键步骤。对于每道题，探索 **至少 2-3 个改编方向**，
记录在 README.md 中。格式：

```markdown
## 改编思路

### 方向一：<简短描述>
- 做法：<具体改编方法>
- 优点：<为什么好>
- 缺点/风险：<为什么不选>
- 结论：✅ 采用 / ❌ 不采用

### 方向二：...
```

你可以自由探索这些典型的改编维度：

| 维度 | 示例 |
|------|------|
| 参数化 | 原题 $n=1000$ → 改为输入 $n$，求 $\le n$ 的结果 |
| 泛化 | 原题求 $3$ 或 $5$ 的倍数 → 改为求 $a$ 或 $b$ 的倍数 |
| 多组询问 | 单次查询 → $T$ 组测试数据 |
| 上界扩大 | $n \le 10^3$ → $n \le 10^{12}$，迫使更优算法 |
| 输出变化 | 输出答案本身 → 输出 $\bmod\ M$ 的结果 |
| 特殊情形 | 增加特殊约束（如 $n$ 是质数时的简化版本） |
| 逆向问题 | 原题"给定 $n$ 求 $f(n)$" → "给定 $K$ 求最小的 $n$ 使得 $f(n)\ge K$" |
| 第 K 大/小 | "求最大/小的 X" → "求第 K 大/小的 X" |
| 计数问题 | "求是否存在/求一个解" → "求方案数" |
| 构造/SPJ | 需要输出具体方案而不是数字 |

**重要**：在 README.md 中，所有探索过的方向都要保留（包括未采用的），
并写明取舍理由。这能帮助后续回顾时理解当时的决策。

题面中不需要暴露改编思路；这些仅记录在 README.md 中。

### 第三步：选定方向并确认

在开始写代码前，确认以下信息（若不明确则追问用户）：
- 题目中文标题
- 所属目录编号（如 `011`、`012`……）
- 是否采用多组输入
- 是否需要 SPJ
- 关键数据范围

这些确认后，创建目录并开始实施。

### 第四步：创建工作区

从 `_template/` 复制结构到新目录：

```bash
cp -r _template/ 0XX/
```

然后按以下顺序填充文件：

### 第五步：编写代码文件

按以下顺序编写，每完成一个就编译验证：

1. **`code/std.cpp`**：标准解法。
   - 编译：`cd 0XX && make std`
   - 用 Project Euler 原题的固定输入验证输出正确（可手算或运行一次验证）。

2. **`code/brute.cpp`**：暴力解法。
   - 在小数据范围上保证与 std 输出一致。
   - 不要求高效，只要能在一个合理的范围内暴力枚举即可。
   - 编译：`make brute`

3. **`code/gen.cpp`**：小数据生成器。
   - 接受 argv[1] 作为模式（如 `small`）。
   - 用于 `check.sh` 对拍——生成小范围随机数据。
   - 编译：`make gen`

4. **`code/gen_data.cpp`**：正式数据生成器。
   - 生成 `data/XX.in` 文件（编号从 01 开始，通常 10~20 个）。
   - 必须包含：样例数据、小数据、大数据、边界条件（最小值、最大值）。
   - 编译：`make gen_data`

### 第六步：生成测试数据并验证

```bash
cd 0XX
make data              # 生成 data/*.in → data/*.out
./eval.sh              # 检验标程在最大数据点上的运行时间
make check             # 暴力对拍（小数据范围）
```

如果对拍失败，找到 WA 的 case，修代码，重新编译 + 对拍到通过。

### 第七步：编写 README.md

README.md 格式（参考 001/README.md）：

```markdown
# English Title

来源：改编自 Project Euler Problem X

## 原题意

<从 source/_posts/ 提取的原题描述，用中文>

## 题目

<改编后的题目简述>

## 数据范围

<数学格式写数据范围>

## 改编思路

### 方向一：...
...

## 算法

<算法简述，复杂度>
```

必须包含：原题意、改编后题目、数据范围、改编思路（含多个方向的取舍）、算法。

### 第八步：编写 statement.md

题面（参考 001/statement.md）：

```markdown
# 中文标题

## 题目描述

<题目描述>

---

## 输入格式

<输入格式>

---

## 输出格式

<输出格式>

---

## 样例

### 输入
​```
<样例输入>
​```

### 输出
​```
<样例输出>
​```

---

## 数据范围

<完整数据范围，覆盖每个变量>
```

题面书写规则：
- 题目描述阶段就让选手理解任务；不要把要求推迟到输入输出格式才出现。
- 数学公式用 `$x$` 和 `$$x$$`；不要用 `\(...\)` 或 `\[...\]`。
- 数据范围覆盖每一个输入变量。
- 样例要有强度，覆盖边界和典型情况。
- 样例解释的职责是帮助理解输入输出，不是提前泄漏解题关键。

### 第九步：最终验证

```bash
cd 0XX
make clean && make all && make data && make check && ./eval.sh
```

确保：
- std 在最大数据上运行时间合理（不超过 1 秒为宜）
- check 对拍通过（至少 100 轮）
- 所有 `data/*.in` 和 `data/*.out` 都存在
- README.md 和 statement.md 完整无占位符

## 改编策略原则

遵循 `README.md`（项目根目录）中的策略：

1. **不改变原题难度**作为首要原则。改编是为了让题目适合 OJ 评测，
   不是为了增加难度。
2. **将固定输入参数化**是核心改编方法。原 Project Euler 题目通常
   只有一个固定输入（如 $n=1000$），将其改为可变的输入参数。
3. **多组输入是可选的**。对于简单题，单组输入更清晰；对于需要
   展示不同情形的题，多组输入更合适。根据具体题目判断。
4. **避免引入不必要的高难度技巧**。对于原题 level 较低（简单）的题，
   不引入逆元、大数运算、复杂数据结构等会显著提高难度的元素。
5. **SPJ 是例外而非规则**。仅在确实需要时才使用（如输出不唯一的情形）。
6. **数据范围的设计要配套算法**。如果原题的朴素算法是 $O(n)$，
   将 $n$ 扩大到让朴素算法超时，但核心算法思想不变。

## 注意事项

- 改编的核心是"参数化"而非"重新发明"。保留 Project Euler 原题
  的核心数学模型和算法挑战。
- `gen.cpp` 和 `gen_data.cpp` 的区别：前者用于对拍（小数据），
  后者用于正式测试数据（全范围）。
- `check.sh` 的对拍只在小数据范围上进行（因为 brute 无法处理大数据）。
- Makefile 中的 `CXXFLAGS` 使用 `-std=c++17 -O2`。
- 所有脚本使用 bash（不是 PowerShell）。
- 不要改动 `_template/` 中的文件——它是所有题目的起点。
- 如果 `eval.sh` 显示标程运行时间过长，检查算法是否达到预期复杂度。

## 验收清单

结束前确认：
- [ ] 原题已从 `source/_posts/` 正确读取并理解
- [ ] README.md 包含原题意 + 改编后题目 + 多个改编方向（含取舍理由）+ 算法
- [ ] statement.md 包含完整的题面（描述 + 输入输出 + 样例 + 数据范围）
- [ ] `code/std.cpp` 能通过 Project Euler 原题的固定输入验证
- [ ] `code/brute.cpp` 在暴力范围内与 std 一致
- [ ] `code/gen_data.cpp` 生成了充足的测试数据（含边界）
- [ ] `make check` 对拍通过
- [ ] `./eval.sh` 运行时间合理
- [ ] 所有文件无 TODO、占位符、空实现
