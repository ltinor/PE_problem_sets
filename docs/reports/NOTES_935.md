# PE935 翻滚正方形分析笔记 (2026-09-19)

## 已完成: 定义忠实的逐步模拟器 (锚点全通过)

`_tools/audit/_work/t935_sim.cpp` (单点) 与 `t935_scan.cpp` (批量):
- 状态 = 4 角坐标 + 枢轴角索引; 每步绕枢轴顺时针旋转至某非枢轴角首次触边;
- 同时落地取多候选, 新枢轴 = "顺时针旋转能留在内部"的可行候选
  (速度 v=(dy,-dx), 对每个触边角的 v·外法线 <= 容差), 优先刚落地角;
- 回归 = 角坐标集合回到初始集合 (方向/标号无关)。

验证 (全部精确复现):
- b=1/2 -> 4 步 (90°x4, 绕大正方形中心的对角翻滚);
- b=2-sqrt2 -> 4 步 (45°x4);
- b=2+sqrt2-sqrt(2+4sqrt2) -> 4 步 (33.047°, 11.953°, 11.953°, 33.047°, 总 90°);
- b=8-5sqrt2+4sqrt3-3sqrt6 -> 6 步 (75°,15°,45°,45°,15°,75°, 总 270°);
- b=5/13 -> 24 步 (官方陈述)。

注意: b 需要完整精度输入 (1e-5 截断即不回归); 浮点实现需坐标吸附+可行性容差。

## 结构观察

1. **回归步序列的步角对称**: (α,β,β,α) / (75,15,45,45,15,75) 等, 关于中点镜像。
2. **总旋转角 = 90° 的整数倍** ( Orientation mod 90° 恢复的必要条件)。
3. b=1/2^k 族: 步数 = 4(2^k - 1) (k=1,2,3,4 -> 4,12,28,60, 已模拟验证)。
4. 有理 b=p/q 时所有步角的正切都是有理数 (接触几何坐标全有理)。
5. 闭轨 b 值既有有理数 (1/2, 5/13) 也有二次无理数 (2-sqrt2 等)。

## 计数猜想 (未证明, 与官方量级吻合)

F(1e8) = 759908921637225 ≈ 0.076·N^2 = (1/4)·(3/π^2)·N^2 ~ (1/4)·Σ_{q<=N} φ(q)。
若闭轨 b 由两个互素整数参数 (类似角度 tan = p/q) 参数化且首回归步数 ~ 某个
分母量级, 则 F(N) 应是 φ 求和型的二次多项式 (可能有低阶修正项)。
N=100: Σφ(q)/4 = 761 vs F(100)=805 — 差 44, 说明有额外族或修正。

## 下一步

1. 用模拟器对 (步数 k, 序号) 系统化枚举小步数闭轨, 提取 b 的代数式, 找
   (k, m) -> b 的显式参数化 (猜想与 tan(90°m/k) 型角度有关)。
2. 确定首回归 = k 的参数计数 (排除非首回归), 得 F(N) 的精确公式。
3. 公式化后 O(小) 计算 F(1e8), 对拍官方 759908921637225 与 F(100)=805。

## 格点吸附实验与真结构 (2026-09-24)

### 实验结论
1. 有理 b = p/q 的动力学保持在 1/q 格点上当且仅当旋转角为格点保持旋转
   (90° 或 Pythagorean 方向翻转, 后者要求 p 为勾股弦数, 即 p 有 4mod1 素因子).
2. 格点吸附模拟对 "可丢番图逼近的 p/q" 会制造伪回归 (如 b=3/5: 吸附后 4 步回归,
   无吸附高精度模拟正确地不回归) —— 吸附扫描不可用于枚举.
3. 真回归集 = 孤立代数数 (嵌套根式), 每个轨道模式 (接触类型序列) 给出多项式
   闭合方程, 解出有限个 b. 原高精度模拟器 (无吸附) 是唯一可靠的验证工具.

### 渐近结构 (数值确认)
F(N) ~ 3N^2/(4pi^2) + 低阶修正:
- N=100: 759.9 vs 805 (+45);
- N=1e8: 7.5989e14 vs 759908921637225 (+~1e10, 相对偏差 2e-5).
系数 3/(4pi^2) = (1/4)*(6/pi^2) 与 totient 求和的结构猜想一致:
F(N) ≈ (1/4) sum_{q<=N} phi(q), 修正项来自小步数模式族.

### 完整解的路线 (未完)
按轨道模式枚举: 每个模式 (如 4 步族的 (a, 45°-a, 45°-a, a)) 给出 b 的
多项式方程 + 闭合量子条件, 消元得参数化; 步数 <= N 的参数范围计数,
全体模式求和 (注意去重: 同一 b 可能有多模式回归), 应得精确 totient 型公式,
终值 759908921637225. 主要工作量: 模式分类的完备性证明 (可用模拟器抽样校验).

### 工具状态
- t935_sim.cpp: 高精度逐 b 模拟 (锚点全验证), 接触检测容差敏感 (b 需全精度输入),
  对格点分数 b (如 1/3) 会在长轨道后因浮点漂移失效 —— 精确枚举需有理算术版.
- t935_scan3.cpp: 格点吸附扫描 (仅用于轴对齐/Pythagorean 族的合法枚举, 会伪回归).
