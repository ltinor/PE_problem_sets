# 全量题目审计报告（001-968）

- 审计日期：2026-09-16
- 环境：Windows 11 + WinLibs GCC 16.2.0（`-std=c++17 -O2`），8 并发，全量耗时约 4.5 分钟
- 方法与工具：`_tools/audit/audit.py`（001-010 试点）、`_tools/audit/audit_full.py`（011-968 全量）
  1. **编译** std.cpp；
  2. **数据吻合**：逐点运行 `data/*.in` 与 `*.out` 比对（含耗时，单点上限 10s）；
  3. **PE 探针**：451 题源码含 `"PE"` 查询分支，喂 `PE` 取输出末行，与源码内嵌答案、`_tools` 硬编码官方答案字典（139 题）双重比对（上限 30s）；
  4. **题面检查**：statement.md 空模板 / 缺样例 / 缺数据范围（启发式）；题面语义为人工抽查。
- 原始数据：`_tools/audit/result_001-010.json`、`result_011-968.json`、`answers_official.json`

## 一、总体结论

| 检查项 | 通过 | 问题 |
| --- | --- | --- |
| 编译（968 题） | 951 | **17** 题失败（全部为 `M_PI`/`M_E` 数学常量方言问题，`-std=gnu++17` 下 17/17 全过，非逻辑错误） |
| 数据吻合（245 题有 data） | 202 | **43** 题：19 题 WA、23 题 `.out` 缺失、5 题数据点 TLE |
| PE 探针（451 题执行） | 410 | **41** 题：4 占位、4 异常输出、3 真性答案不符、31 无内嵌答案可比（其中 929/931 慢、461 TLE 另计） |
| 题面（968 题） | 332 | **627** 空模板、4 无样例、5 无数据范围（另有试点发现的 003/004/008/009 题面缺陷） |

**自动检查干净的题目约 948 道；需要处理的问题按优先级列示如下。**

## 二、P0：正确性问题

### A. 编译失败 17 题（可移植性，非逻辑错误）
`177, 246, 285, 380, 575, 613, 769, 777, 779, 780, 807, 843, 844, 880, 894, 899, 900`
- 全部为 `M_PI`/`M_E` 未声明：严格 `-std=c++17` 下 `<cmath>` 不暴露 POSIX 数学常量。
- 实测 `-std=gnu++17` 全部通过。**修复**：审计/评测统一改用 `-std=gnu++17`，或在源码补 `#define _USE_MATH_DEFINES` / 自定义常量。

### B. 数据 WA 19 题（std 与 .out 不吻合，需逐题归因）
`057, 058, 059, 133, 147, 160, 180, 181, 184, 189, 196, 198, 200, 209, 234, 235, 267, 307, 323`
- 其中 058/209/267/200 为部分 WA（1~6 个点），其余几乎全 WA。
- 234、235 与历史坏题清单重合（见第四节），大概率是真实算法 bug 而非数据过期。

### C. 数据 TLE 5 题
`046（3 点 >10s）、185、198、212、305` —— 212 亦在历史坏题清单中。

### D. 数据缺 `.out` 23 题（.in 生成过但答案文件未生成/丢失）
`046, 054, 056, 156, 174, 185, 206, 281, 291, 293, 297, 305, 376, 377, 378, 379, 381, 396, 762, 945, 955, 957, 960`
- **修复**：确认 std 正确后 `make data` 批量补生成。

### E. PE 探针异常 11 题
- **占位答案（输出 "0"）**：`730, 902, 919, 951`
- **异常输出**：`918`（输出负数 -6999033352333308，PE 答案不可能为负）、`836`（输出 "apr2024"）、`933/935`（输出文字说明而非数值）
- **真性答案不符（探针 vs 内嵌常量，且无官方答案可仲裁）**：`369`（got 880917276 vs 内嵌 8624002）、`683`（科学计数法 `2.38955315e11`）、`714`（`2.452353775700e15`，且历史报告曾记录其输出占位）
- 另 `462` 输出科学计数法 `5.5350769703e1512`，精度受损，建议改定点大数输出。

### F. 性能问题 4 题
- `461`：探针 TLE（≥30s）
- `929`：15.2s、`931`：1.45s（>1s 惯例时限）
- 试点发现：`006` K≥7.8 万溢出（正确性）、`007` K=10⁶~10⁷ 约 1.9~2.0s

### G. 试点遗留（001-010，详见 AUDIT_001-010.md）
- `002`：data 7 点损坏（std 正确，需重新 `make data`）
- `007`：data 含越界 K=0 且 `.out` 陈旧
- `006`：溢出 bug（如上）

## 三、P1：题面质量

- **627 题空模板**（<400 字节，章节全空）：`018-020, 041-043, 077-078, 172-175, 244-245, 252-260, 268-270, 273-280, 297-298, 300, 308-315, 326-340, 346-365, 369-370, 375, 386-390, 401-455, 457-560, 566-761, 763-935, 938, 940-944, 951-952, 958-959, 963`
- **无样例**：`012, 014, 198, 395`；**无数据范围**：`068, 090, 142, 144, 271`
- 试点人工发现：`003` 标题错误、`004` 样例缺失、`008` 样例输出错误 + 约束自相矛盾、`009` 声称任意解但无 SPJ checker
- 说明：题面歧义/语义错误无法全自动判定，空模板需批量补写（可依据各题 `README.md`/`source/_posts/` 原题半自动生成）。

## 四、P2：元数据与历史对照

- **内嵌 PE 答案过期 34 题**（探针输出实为完整小数，内嵌常量为截断/占位旧值）：`593, 595, 597, 607, 610, 640, 644, 667, 687, 689, 695, 697, 701, 727, 729, 756, 901, 903, 796, 798, 815, 816, 823, 824, 826, 837, 840, 865, 869, 887, 889, 893, 896, 898` —— 代码本身输出更新，建议回填常量。
- **与 `VERIFY_REPORT.md`（历史报告）对照**：
  - 历史算法 bug 12 题（202,208,212,221,234,235,353,408,411,424,445,764）：本次复现 **212（TLE）、234、235（WA）**；其余 9 题未见异常。注意 PE 探针只验证 PE 分支，参数化路径的 bug（"PE 对、参数错"型）仍需这 9 题人工复查。
  - 历史 TLE 18 题：本次仅 461/929/931，其余已消失。
  - 历史占位 43 题：本次仅剩 4 题 + 933/935 文字输出，大幅改善。
  - 历史编译错 10 题已全部修复；新出现 17 题 M_PI 方言问题。

## 五、修复优先级建议

1. **P0-A**（最省事）：统一 `-std=gnu++17` 或修 17 题源码 → 编译通过率 100%。
2. **P0-B**：19 题 data WA 逐题归因（std 错 → 修代码；.out 错 → 重新 `make data`），可先用 Python 暴力对拍辅助定位。
3. **P0-C**：11 题探针异常（730/902/919/951/918/836/933/935/369/683/714）逐题修复。
4. **P0-D**：23 题补 `.out`。
5. **P1**：性能 4 题 + 006 溢出修复；题面批量补写（627 空模板建议按题号分段流水处理）。
6. **P2**：回填 34 题内嵌答案；统一各题 `eval.sh` 的 python3 依赖与时限声明。

## 六、复跑方式

```bash
python _tools/audit/audit.py 001 010        # 试点范围
python _tools/audit/audit_full.py 011 968   # 全量（约 4.5 分钟）
```
