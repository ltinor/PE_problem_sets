# 平移指数（Shifted Exponentiation）

## 题目描述

对于给定的非负整数 $k$ 和正整数 $n$，定义**平移指数**运算为 $(n + k)^n$，即底数被平移 $k$ 后的自幂运算。

求 $\displaystyle \sum_{n=1}^{N} (n + k)^n \bmod M$ 的值，其中 $N, k, M$ 为给定的参数。

## 输入格式

无输入（固定参数问题）。

## 输出格式

输出一个整数，表示所求之和模 $M$ 的结果。

## 样例

（无样例输入输出，直接计算并输出最终答案。）
