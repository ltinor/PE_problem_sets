# 三角形三元组

## 题目描述

第 n 个三角形数 T(n) = n(n+1)/2。dT(n) 为 T(n) 的约数个数。

Tr(n) 为满足 1≤i<j<k≤n 且 dT(i)>dT(j)>dT(k) 的三元组 (i,j,k) 数量。

给定 n，求 Tr(n) 的后 18 位数字。

---

## 输入格式

一个整数 n。

---

## 输出格式

一个整数 Tr(n) mod 10^18。

---

## 样例

### 输入
```
100
```

### 输出
```
5772
```

---

## 数据范围

1 ≤ n ≤ 10^5
