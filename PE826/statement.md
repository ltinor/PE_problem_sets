# 电线上的鸟（Birds on a Wire）

## 题目描述

一根长度为 $L$ 的电线上，有 $N$ 只鸟依次随机降落。每只鸟在电线上均匀随机地选择一个位置。如果新降落的鸟与某只已在电线上的鸟之间的距离小于 $d$，则两只鸟都会受惊飞走（新鸟不留下，已有鸟也飞走）。如果新鸟与多只已有鸟的距离都小于 $d$，则所有受影响的鸟都飞走。

求最终电线上剩余的鸟的期望数量。

## 输入格式

无输入（固定参数问题）。

## 输出格式

输出一个浮点数，保留 8 位小数，表示期望剩余鸟数。

## 样例

（无样例输入输出，直接计算并输出最终答案。）
