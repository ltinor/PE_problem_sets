# 迭代组合（Iterated Composition）

## 题目描述

设 f 是从集合 {1, 2, ..., N} 到自身的函数。定义迭代组合 fᵏ 如下：

- f¹(x) = f(x)
- fᵏ(x) = f(fᵏ⁻¹(x))，当 k ≥ 2

研究 N 个元素的函数在迭代组合下的性质。计算满足特定条件的函数数量。

## 输入格式

无输入（固定参数问题）。

## 输出格式

输出一个整数，表示所求的函数数量。

## 样例

（无样例输入输出，直接计算并输出最终答案。）
