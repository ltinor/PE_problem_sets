# 排序数字（Sorted Digits）

## 题目描述

如果一个正整数的十进制表示中，各位数字从左到右是非严格递增的（即 $d_1 \le d_2 \le \cdots \le d_k$，其中 $d_1 \neq 0$），则称该数为**排序数字**。

例如：$1, 2, 11, 12, 113, 123, 999, 111222$ 都是排序数字。

请计算不超过某个界限的所有排序数字的相关统计量（个数、和等）。

## 输入格式

无输入（固定参数问题）。

## 输出格式

输出一个整数，表示最终答案。

## 样例

（无样例输入输出，直接计算并输出最终答案。）
