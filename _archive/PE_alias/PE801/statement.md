# 公正矩阵（Equable Matrices）

## 题目描述

对于一个 n×n 的矩阵，如果它满足以下条件，则称为"公正矩阵"：

1. 矩阵的每个元素取自集合 {1, 2, ..., n}。
2. 所有 n 行的元素和都相等，且所有 n 列的元素和也等于同一值。即存在一个公共值 S，使得每行之和 = 每列之和 = S。

令 f(n) 表示所有 n×n 公正矩阵的数量。求 f(M) 的值。

## 输入格式

无输入（固定参数问题）。

## 输出格式

输出一个整数，表示 f(M) 的值。

## 样例

（无样例输入输出，直接计算并输出最终答案。）
