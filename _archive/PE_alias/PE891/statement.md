# 无歧义时钟（Unambiguous Clock）

## 题目描述

在一个模拟时钟上，时针和分针的外观完全相同（不可区分）。仅通过观察两根指针的位置，你能否唯一确定当前时间？

在大多数情况下，将时针和分针的位置互换会得到另一个有效的时间读数，因此存在歧义。但在某些特殊时刻，交换后不产生有效时间，这些时刻称为**无歧义时刻**。

请计算在 12 小时周期内，有多少个不同的分钟时刻是无歧义的。

## 输入格式

无输入（固定参数问题）。

## 输出格式

输出一个整数，表示最终答案。

## 样例

（无样例输入输出，直接计算并输出最终答案。）
