#include<bits/stdc++.h>
using namespace std;

int main(int argc, char* argv[]) {
    srand(time(0));
    // Any integer input works - output is always the same
    cout << rand() % 1000 << "\n";
    return 0;
}
