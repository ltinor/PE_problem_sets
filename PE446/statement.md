# 撤销函数 B

## 题目描述

对于任意整数 n>1，定义函数 f(x)≡ax+b (mod n)，其中 a,b,x 均为整数，
满足 0<a<n，0≤b<n，0≤x<n。

若对所有 0≤x<n 均有 f(f(x))≡f(x) (mod n)，则称 f 为**撤销函数**。

记 R(n) 为模 n 意义下撤销函数的个数。

定义 F(N)=∑_{n=1}^{N} R(n⁴+4)。

已知 F(1024)=77532377300600。

---

## 输入格式

一个整数 N。

---

## 输出格式

一个整数，表示 F(N)。对于 N=10⁷，输出结果模 1000000007。

---

## 样例

### 输入
```
10
```

### 输出
```
...
```

---

## 数据范围

- 对于 30% 数据：N ≤ 10
- 对于 60% 数据：N ≤ 1000
- 对于 100% 数据：N ≤ 2000（小规模）或 N = 10⁷（PE 答案硬编码）
