# 约数对

## 题目描述

记 S(n) 为 n 的不同约数组成的约数对 (a,b) 的数目，其中 a 整除 b。
记 p_m# 是前 m 个素数的乘积。记 E(m,n) 为使得 2^k 整除 S((p_m#)^n) 的最大整数 k。
记 Q(n) = Σ_{i=1}^n E(904961, i)。

已知 Q(8) = 2714886。

给定 N，求 Q(N)。

---

## 输入格式

一行，一个整数 N。

---

## 输出格式

一行，一个整数 Q(N)。

---

## 样例

### 输入
```
8
```

### 输出
```
2714886
```

---

## 数据范围

- 1 ≤ N ≤ 10^12
