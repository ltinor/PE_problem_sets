# 循环多项式（Cyclogenic Polynomials）

## 题目描述

首一多项式是指最高次项系数为 1 的单变量多项式。定义 $\mathcal{F}$ 为所有整系数首一多项式（包括常数多项式 $p(x)=1$）构成的集合。

对于多项式 $p(x) \in \mathcal{F}$，若存在 $q(x) \in \mathcal{F}$ 和正整数 $n$ 使得 $p(x)q(x) = x^n - 1$，则称 $p(x)$ 为**成圆多项式**（cyclogenic polynomial）。若 $n$ 是满足上述条件的最小正整数，则称 $p(x)$ 为 $n$**-成圆多项式**。

定义 $P_n(x)$ 为所有 $n$-成圆多项式的和，$Q_N(x) = \sum_{n=1}^{N} P_n(x)$。

已知 $Q_{10}(2) = 5598$。

求 $Q_{10^7}(2) \bmod 1{,}000{,}000{,}007$。

## 输入格式

无输入（固定参数问题）。

## 输出格式

输出一个整数，表示 $Q_{10^7}(2)$ 对 $1{,}000{,}000{,}007$ 取模的结果。

## 样例

（无样例输入输出，直接计算并输出最终答案。）
