# 平移倍数（Shifted Multiples）

## 题目描述

对于一个十进制正整数 s，定义一次"平移"操作：将 s 的最后一位数字移到最前面（即循环右移一位）。设平移后的数字为 M。

如果存在整数 k > 1，使得 M = k·s（即 M 是 s 的整数倍），则称 s 为"平移倍数"。

求所有不超过给定上界的平移倍数之和。

## 输入格式

无输入（固定参数问题）。

## 输出格式

输出一个整数，表示所有满足条件的整数之和。

## 样例

（无样例输入输出，直接计算并输出最终答案。）
